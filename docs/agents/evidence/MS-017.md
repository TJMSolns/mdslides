Commit: `a40c4db` (MS-017: typed SlotName ADT (enum, parser/renderer boundary enforcement)),
on branch `main`.

Run-count: `mill __.compile` clean under `-Xfatal-warnings` (full clean rebuild, all 5 modules:
domain, infrastructure, cli, domain.test, infrastructure.test, mcp, mcp.test — 0 warnings, 0
errors). `mill __.test` — 687 tests pass, 0 failures, exit code 0 (full suite: domain + property
suites, infrastructure incl. HTMLRenderer/SpeakerViewRenderer/MarkdownParser specs, cli, mcp).

What changed (per `doc/internal/planning/design-MS-017-typed-slot-name.md`'s approved "Proposed
mechanism," items 1-7):

1. `domain/.../SlotName.scala` — replaced the dead `opaque type SlotName = String` with
   `enum SlotName derives CanEqual { case Title, Subtitle, Author, Heading, Body, Caption,
   LeftColumn, RightColumn }`, plus `SlotName.fromString(String): Either[String, SlotName]` and
   an extension `.value: String` for the JSON/Markdown string boundary.
2. `domain/.../SlotDefinition.scala` — `name: String` -> `name: SlotName`; all 7 `SlotDefinition`
   vals (`.title`, `.subtitle`, `.author`, `.heading`, `.body`, `.caption`, `.bodyOptional`) now
   pass the enum case. (Correction: an earlier draft of this artifact said "8 vals" — that was
   wrong; `SlotDefinition.scala` has 7 named vals. `SlotName.LeftColumn`/`.RightColumn` — the
   remaining 2 of the 8 enum cases — have no dedicated `SlotDefinition` val; they're constructed
   inline in `Template.scala`'s `two-column` template, per item 3 below.)
3. `domain/.../Slide.scala` — `slots: Map[String, String]` -> `Map[SlotName, String]`;
   `getSlot`/`hasSlot`/`slotNames` retyped to `SlotName`. `domain/.../Template.scala` —
   `getSlot(name: String)` -> `getSlot(name: SlotName)`; the `two-column` template's
   `SlotDefinition("leftColumn", ...)`/`("rightColumn", ...)` literals now pass
   `SlotName.LeftColumn`/`SlotName.RightColumn`.
4. `infrastructure/.../parser/MarkdownParser.scala` — `parseTitleSlide`/`parseContentSlide`/
   `parseTwoColumnSlide` now build `Map[SlotName, String]` directly (`SlotName.Title -> ...`,
   etc.) instead of string-keyed maps; a typo like `SlotName.Heding` fails to compile, whereas
   the equivalent `"heding"` previously type-checked silently (MS-005/HL-002's exact failure
   mode).
5. `infrastructure/.../renderer/HTMLRenderer.scala` — all 17 content-slot `slide.getSlot("x")`
   call sites (title/subtitle/author/heading/body/caption/leftColumn/rightColumn, across
   `renderTitleSlide`/`renderContentSlide`/`renderDiagramSlide`/`renderClosingSlide`/
   `renderSectionTitleSlide`/`renderTwoColumnSlide`) now pass `SlotName.X`.
   `infrastructure/.../rendering/SpeakerViewRenderer.scala` — the 2 `getSlot("title"/"heading")`
   calls in `encodeSlidesAsJson` likewise migrated.
6. `header`/`footer`/`vertical-align` frontmatter keys are unchanged as raw `String`, per Q2(a) —
   `ConfigLoader.scala`, `ThemeJsonAdapter.scala`, `HeaderFooter.scala` not touched.
7. Test fallout: all 9 files named in the design note (`SlideSpec`, `TemplateV2Spec`,
   `MarkdownParserSpec`, `HTMLRendererSpec`, `HTMLRendererTemplateV2Spec`, `HTMLRendererTableSpec`,
   `HTMLRendererImageSpec`, `HTMLRendererTwoColumnSpec`, `ContentSlideSpec`) plus the 4 named
   generators/properties (`DomainGenerators.scala`, `SlideProperties.scala`,
   `SlideDeckProperties.scala` — unaffected, doesn't construct slot maps directly,
   `ContentSlideProperties.scala`) updated to construct `Slide`/`SlotDefinition` with `SlotName`
   cases. **Six additional files** not named in the design note's list, but which construct
   `Slide`/`Map[String,String]` slot literals directly and would not otherwise compile, were also
   updated on the same mechanical basis (see full corrected list in the deviations section below):
   `SlideDeckSpec.scala`, `TemplateContentValidationSpec.scala`, `HTMLRendererGoogleFontsSpec.scala`,
   `HTMLRendererLiveReloadSpec.scala`, `HTMLRendererOverflowSpec.scala`, and
   `SpeakerViewRendererSpec.scala`. (Correction: an earlier draft of this artifact named only the
   first two of these six — that undercounted the real list by four files, an evidence-completeness
   defect caught by the independent verifier's first pass, VETO'd, and fixed here.)

Deviations from the design note's literal wording (disclosed, not silently improvised):

- **Frontmatter metadata channel.** Item 3 says `Slide.slots` becomes fully `Map[SlotName,
  String]`; item 6 says `header`/`footer`/`vertical-align` stay raw strings with "no change to...
  the 3 literal frontmatter reads in `MarkdownParser.scala:154-156` / `HTMLRenderer.scala:707,
  730,749`." These two clauses are in tension: those 3 reads use the *same* `slots` map and
  `getSlot` method the content slots use, and Scala can't hold both `SlotName` and raw-`String`
  keys in one `Map`. Resolution (mechanical, not a scope change — Q2a's actual decision, "leave
  them untyped," is fully preserved): added a second field `metadata: Map[String, String] =
  Map.empty` to `Slide`, plus an overloaded `getSlot(key: String): Option[String]` reading from
  it. This let `HTMLRenderer.scala`'s 3 cited call sites (`slide.getSlot("vertical-align")`,
  `slide.getSlot("header")`, `slide.getSlot("footer")`) resolve via the `String` overload with
  **zero edits** to those lines. `MarkdownParser.scala`'s frontmatter-assignment lines
  (152-156) did need touching — `baseSlots`'s type itself changed, so the `title` override
  (genuine content, `SlotName.Title`) and the 3 metadata keys (now written to the new
  `metadata` map) had to split into two collections. No compile-time enforcement was added to
  the metadata channel; behavior for header/footer/vertical-align (priority order, optionality,
  raw-string values) is unchanged.
- **Color map left String-keyed.** Item 5 additionally says "the color-map keyed by slot name
  (`HTMLRenderer.scala:686-689`) becomes `Map[SlotName, ...]`." Not implemented: that map
  (`resolveColors`) is passed to `TemplateColors.mergeWithDefaults(themeDefaults: Map[String,
  String]): Map[String, String]` (domain, `TemplateColors.scala`), which in turn is
  JSON-decoded via `ThemeJsonAdapter.scala` — both explicitly out of MS-017's approved scope.
  Retyping the color map would force retyping `TemplateColors.mergeWithDefaults` too, rippling
  into a file the design note didn't authorize touching. This map's producer and consumer are
  co-located in the same function/file already (not a parser->renderer boundary — never the
  site of the LL-003 failure mode), so there's no compile-time-safety loss from leaving it
  String-keyed.
- **`Slide.validateTemplateSpecificContent`'s `parsedSlots` param** (not named in the design
  note) retyped `Map[String, FormattedContent]` -> `Map[SlotName, FormattedContent]`. Its one
  caller (`TemplateContentValidationSpec.scala`) builds `parsedSlots` by mapping directly over
  `slide.slots` (now `SlotName`-keyed), so this follows mechanically from item 3's retype with
  no design decision involved.
- **Six test files not on the design note's list of ~12 were also updated** — each constructs
  `Slide` values (or, for `TemplateContentValidationSpec.scala`, `Slide.validateTemplateSpecificContent`'s
  `parsedSlots` argument) with literal slot-map keys directly and would not otherwise compile once
  `Slide.slots` retyped: `SlideDeckSpec.scala`, `TemplateContentValidationSpec.scala`,
  `HTMLRendererGoogleFontsSpec.scala`, `HTMLRendererLiveReloadSpec.scala`,
  `HTMLRendererOverflowSpec.scala`, `SpeakerViewRendererSpec.scala`. Same mechanical treatment as
  the files the design note did name (literal `"title"`/`"heading"`/etc. map keys and
  `getSlot("...")` calls converted to `SlotName.X`); no design decision involved in any of the six.
  **Correction:** the first version of this evidence artifact disclosed only the first two of
  these six files under this heading, describing them as "two additional files" — that was
  incomplete and inaccurate as written (it read as an exhaustive list but wasn't). The independent
  verifier caught this on its first pass (VETO), diffing the full commit itself rather than
  trusting this artifact's prose; all six are named here now, and the commit itself was correct
  and unchanged throughout — only this disclosure text was deficient.

No changes made to `ConfigLoader.scala`, `ThemeJsonAdapter.scala`, `HeaderFooter.scala`,
`TemplateColors.scala`, or any frontmatter-key semantics — all out of MS-017's approved scope per
Q2(a), confirmed by `grep` before and after.

Invariance-recheck: re-read `Slide.scala`, `SlotDefinition.scala`, `Template.scala`,
`MarkdownParser.scala`, `HTMLRenderer.scala`, `SpeakerViewRenderer.scala` in full before editing,
confirmed the exact call sites the design note cited (`HTMLRenderer.scala:707,730,749` for
vertical-align/header/footer; `:686-689` for the color map; `MarkdownParser.scala:152-156` for
frontmatter assembly) against the real line numbers rather than assuming they matched. Grepped the
whole tree for `getSlot(`/`hasSlot(`/`SlotDefinction(`-style constructors after the change to
confirm no remaining `String`-typed content-slot call site was missed. Ran a clean `mill clean &&
mill __.compile` (not incremental) to confirm no warning/error was masked by stale incremental
state.

Verified-by: Claude (doer, sonnet) — re-read all cited source files and re-ran the full test suite
from a clean build before dispatching the independent verifier below.

Verifier-tier: sonnet (drawn via `python3 .claude/hooks/draw-verifier-tier.py P4 sonnet` — canonical
script, tier=sonnet raw_offset=0 final_offset=0 resampled=False)

Verifier-round-1-result: **VETO** — independent verifier (Sonnet) independently confirmed both
commits real, confirmed the core implementation matches the design note faithfully (genuine `enum`,
correct retypes, exactly 17 + 2 `SlotName.X` call-site conversions in `HTMLRenderer.scala`/
`SpeakerViewRenderer.scala`, exact match on `mill clean && mill __.compile` clean and `mill __.test`
687/687 pass reproduced independently), confirmed the Q2a metadata-channel tension was real and the
resolution faithful, confirmed the color-map/`TemplateColors`/`ThemeJsonAdapter` scope boundary held.
VETO reason: this evidence artifact's first draft disclosed only 2 of the 6 test files touched
beyond the design note's list (omitted `HTMLRendererGoogleFontsSpec.scala`,
`HTMLRendererLiveReloadSpec.scala`, `HTMLRendererOverflowSpec.scala`, `SpeakerViewRendererSpec.scala`),
and miscounted `SlotDefinition`'s vals as 8 instead of 7 — a disclosure-completeness defect in this
artifact, not a code defect. Fixed above (items 2 and 7, and the deviations section) with the full
six-file list and the corrected 7-val count; no code change was needed or made. Resubmitting for
round 2.

Verifier-round-2-result: **PASS** — independent verifier (Sonnet), fresh dispatch with no
shared context, independently re-derived the "touched but not named in the design note" file set
from `git show a40c4db --stat` cross-referenced against the design note's named list and got an
exact match to the corrected six-file list (`SlideDeckSpec.scala`,
`TemplateContentValidationSpec.scala`, `HTMLRendererGoogleFontsSpec.scala`,
`HTMLRendererLiveReloadSpec.scala`, `HTMLRendererOverflowSpec.scala`,
`SpeakerViewRendererSpec.scala`) — no more, no fewer; independently read `SlotDefinition.scala` and
confirmed exactly 7 named vals, matching the corrected text; confirmed `1abd734` (the correction
commit) touches only this evidence artifact file (44 insertions/12 deletions, nothing else);
confirmed no `.scala` file changed between `a40c4db` and HEAD (`git log --oneline a40c4db..HEAD --
'*.scala'` empty), so round 1's independently-reproduced clean compile and 687/687 test pass remain
valid without re-running; read the rest of the artifact end-to-end and found no further
inaccuracies.

Verifier-verdict: PASS (round 2, sonnet tier — see Verifier-round-2-result above for the full
independent re-derivation; round 1 VETO'd on evidence-disclosure completeness only, fixed in
commit 1abd734 with no code change, per Verifier-round-1-result above)
