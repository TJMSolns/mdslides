Commit: `06f3b5b` (MS-019: MCP server Tier 2 — list_themes, get_deck_info), on branch `main`.

Run-count: `mill mcp.test` — 18/18 tests pass (10 pre-existing Tier 1 + 8 new Tier 2: tools/list
inclusion, list_themes happy path with no themes_dir, list_themes with a nonexistent themes_dir
(not an error), list_themes with a directory-based theme present, get_deck_info missing-file error,
get_deck_info happy path with two slides/mixed templates/an image/a Mermaid diagram, get_deck_info
on a deck with no Mermaid diagrams). `mill __.test` (full domain + infrastructure + cli + mcp suite)
green, no regressions. `mill __.compile` clean under `-Xfatal-warnings`.

Invariance-recheck: cross-checked every design-doc claim against the real code before and after
implementing, not from memory. Read `mcp/src/.../McpServer.scala` (Tier 1) to confirm the existing
dispatch pattern (`parseParams` → `execute` → JSON-RPC success/error envelope with `-32602`/`-32603`
codes) that Tier 2 needed to match — reused it verbatim for `list_themes`/`get_deck_info`. Read
`infrastructure/.../theme/ThemeLoader.scala:98-111` and confirmed `listAvailableThemes` already
returns `Nil` (not a raised error) for a missing directory — this is why `list_themes`'s design doc
and implementation both treat a missing `themes_dir` as a non-error, degrading to built-ins only,
matching the actual `ThemeLoader` contract rather than an assumed one. Read
`cli/src/.../cli/Main.scala:72-121` (`extractImageUrls`, `extractMermaidDiagrams`) and confirmed
`get_deck_info`'s image/diagram extraction in `GetDeckInfoTool.scala` reproduces the same logic
(`FlexmarkAdapter.parseInlineFormatting` per slot → `contentImages` + `backgroundImage` via
`SlideBackground.getImagePath`, `DiagramElement` pattern match for Mermaid presence) without adding a
compile-time dependency on the `cli` module, per ADR-013's explicit constraint ("mcp has no
compile-time dependency on cli"). Read ADR-013 §Tier 2 and confirmed the implemented tool names,
params (`themes_dir?`, `input_path`), and result shapes (`ThemesResult{themes}`,
`DeckInfo{slideCount,templatesUsed,imagesReferenced,hasMermaidDiagrams}`) match the spec exactly.
Confirmed both new v1.0.7 release assets (`md-slides.jar`, `mdslides-mcp.jar`) are present on
`https://github.com/TJMSolns/MD-Slides/releases/tag/v1.0.7` with a current timestamp
(2026-07-11T04:18:2xZ) via `gh release view v1.0.7 --repo TJMSolns/MD-Slides --json assets`.

Verified-by: Claude (doer, sonnet) — re-read all cited source files directly against the design doc
and the implementation before dispatching the independent verifier below.

Verifier-tier: opus (drawn via `python3 .claude/hooks/draw-verifier-tier.py P4 sonnet` — canonical
script, tier=opus raw_offset=1 final_offset=1 resampled=False)

Verifier-verdict: PENDING
